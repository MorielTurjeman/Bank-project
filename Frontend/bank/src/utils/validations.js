export const isStringEmpty = (str) => str.length === 0 //return t or f if str
export const isNegative = (num) => num <= 0 //return t or f if amount is not zero and only positive
export const isNotNumber = (num) => isNaN(num)
